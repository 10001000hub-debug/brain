# 2026-02-13: Tailscale & Local LLM Setup

## Tailscale & Ollama Connection
- **Tailscale:** Installed on server (`x210-131-219-97`). Connected to user's network.
- **Ollama (User PC):** Accessible at `100.64.180.83:11434`.
  - Connectivity verified (`curl` returns model tags).
  - Config: `llama3.1:latest` available.

## OpenClaw Configuration Troubles
- **Goal:** Use Ollama as a sub-agent (`local`) via `/spawn`.
- **Issue:** `sessions_spawn` returns `forbidden (allowed: none)`.
- **Attempts:**
  - Created `agents.json` with `local` definition.
  - Created `openclaw.json` with `allowAgents: ["local"]`.
  - Tried updating Docker env vars (`ALLOWED_AGENTS`, etc.) via Anti-Gravity.
  - Force-restarted gateway process (PID changed).
- **Status:** Still forbidden. Suspect config path mismatch or internal override.

## Dashboard Access
- **URL:** `https://x210-131-219-97.tail2cbe09.ts.net` (via `tailscale serve`).
- **Issue:** Token mismatch. `OPENCLAW_GATEWAY_TOKEN` env var (`6bda...`) is rejected by the running gateway.
- **Plan:** Force-write a new token to `~/.openclaw/gateway_token` and restart to regain access.
